import React from 'react';

// Previously an empty file, which crashed the entire app on load (imported
// globally by Routes/Store.jsx). Minimal placeholder so the app can load;
// the real screen still needs to be built.
const ManagePurchaseRequestDetailViaReorder = () => {
  return (
    <div className="content" style={{ padding: '40px', textAlign: 'center' }}>
      <h3>Manage Purchase Request (Via Reorder)</h3>
      <p style={{ opacity: 0.7 }}>This screen has not been built yet.</p>
    </div>
  );
};

export default ManagePurchaseRequestDetailViaReorder;